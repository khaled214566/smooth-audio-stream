/**
 * Web stub: jsmediatags' ReactNativeFileReader requires this module; esbuild still resolves it.
 * The reader is never used in the browser (Blob/Xhr readers handle files).
 */
module.exports = {};
